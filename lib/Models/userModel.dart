class UserDetails {
  final fbId;
  final fbName;
  final fbEmail;
  final fbPicUrl;
  final loginType;

  UserDetails(
      {this.fbId, this.fbName, this.fbEmail, this.fbPicUrl, this.loginType});
}
